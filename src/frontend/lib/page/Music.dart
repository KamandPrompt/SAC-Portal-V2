import 'package:flutter/material.dart';

class DeploymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Music Club'),
          centerTitle: true,
          backgroundColor: Colors.black54,
        ),
      );
}
